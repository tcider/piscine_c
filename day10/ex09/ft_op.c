#include "ft_advanced_do_op.h"
#include "ft_opp.h"

int		ft_sub(int x, int y)
{
	return (x - y);
}

int		ft_sub(int x, int y)
{
	return (x - y);
}

int		ft_sub(int x, int y)
{
	return (x - y);
}

int		ft_sub(int x, int y)
{
	return (x - y);
}

int		ft_sub(int x, int y)
{
	return (x - y);
}

int		ft_sub(int x, int y)
{
	return (x - y);
}
